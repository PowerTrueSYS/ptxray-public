{
  "id": "ck-dump-copy-dir",
  "pillar": "security",
  "category": "security",
  "finding_ids": [
    "dump_copy_dir"
  ],
  "captures": [],
  "requires_root": true,
  "read_only": true,
  "commands": [
    "sysdumpdev -l"
  ],
  "tags": [
    "ffiec:II.C.21",
    "stig:V-215407"
  ],
  "blurb": "When a system crashes, AIX writes a memory dump to a primary dump device and then copies that image into a copy directory so the failure can be investigated afterwards. This check reads the dump configuration and confirms both a primary device and a copy directory are set. If either is missing, a crash leaves nothing to analyze and the information needed to find the cause and return to service is lost.",
  "license": "Apache-2.0",
  "artifact": "checks/ck-dump-copy-dir/ck-dump-copy-dir.ksh"
}
